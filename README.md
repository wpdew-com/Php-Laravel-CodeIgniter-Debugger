# 🚀 Laravel, CodeIgniter & WordPress Debugger - Chrome Extension

Chrome розширення для моніторингу та дебагу Laravel/CodeIgniter/WordPress додатків через Side Panel.

## ✨ Можливості

- 📊 **Моніторинг в реальному часі** - відстежування всіх запитів до вашого PHP додатку
- 🗃️ **SQL запити** - підрахунок кількості SQL запитів на кожній сторінці
- 🗄️ **Визначення типу БД** - автоматичне визначення MySQL, PostgreSQL, SQLite, SQL Server, Oracle
- ⏱️ **Час виконання** - вимірювання швидкості роботи вашого додатку
- 💾 **Використання пам'яті** - моніторинг споживання пам'яті
- 🎯 **Інформація про роути** - відображення поточного активного роуту
- 🔄 **Історія запитів** - зберігання останніх 100 запитів
- 🎨 **Чистий інтерфейс** - не змінює DOM вашого сайту, все в бічній панелі
- 🌍 **Багатомовність** - інтерфейс українською, англійською та російською мовами

## � QUICK START

У папці `examples/` знаходяться готові до використання файли для швидкого старту з різними фреймворками:

### 📂 Laravel - [LaravelDebugHeaders.php](examples/LaravelDebugHeaders.php)

**Готовий middleware** для Laravel з підтримкою всіх метрик:
- ✅ Версія Laravel
- ✅ Інформація про роут (name або URI)
- ✅ Кількість SQL запитів (з підтримкою transactions)
- ✅ Тип бази даних (MySQL, PostgreSQL, SQLite, SQL Server)
- ✅ Час виконання в мілісекундах
- ✅ Використання пам'яті (поточне та пікове)
- ✅ HTTP метод та статус код
- ✅ Інформація про черги (pending/failed jobs)

**Як використовувати:**
1. Скопіюйте файл в `app/Http/Middleware/DebugHeaders.php`
2. Зареєструйте middleware в `app/Http/Kernel.php` (тільки для local!)
3. Працює автоматично тільки в local/development оточенні

### 📂 CodeIgniter 4 - [CodeIgniter4DebugHeaders.php](examples/CodeIgniter4DebugHeaders.php)

**Готовий Filter** для CodeIgniter 4:
- ✅ Версія CodeIgniter
- ✅ Інформація про контролер/метод
- ✅ Кількість SQL запитів
- ✅ Тип бази даних з конфігу
- ✅ Час виконання
- ✅ Використання пам'яті
- ✅ HTTP метод та статус

**Як використовувати:**
1. Скопіюйте файл в `app/Filters/DebugHeaders.php`
2. Зареєструйте filter в `app/Config/Filters.php`
3. Працює тільки в development оточенні

### 📂 CodeIgniter 3 - [CodeIgniter3DebugHeaders.php](examples/CodeIgniter3DebugHeaders.php)

**Готовий Hook** для CodeIgniter 3:
- ✅ Версія CodeIgniter
- ✅ Інформація про контролер/метод
- ✅ Кількість SQL запитів з DB profiler
- ✅ Час виконання SQL запитів
- ✅ Тип бази даних
- ✅ Час виконання
- ✅ Використання пам'яті

**Як використовувати:**
1. Скопіюйте файл в `application/hooks/DebugHeaders.php`
2. Увімкніть hooks в `application/config/config.php`: `$config['enable_hooks'] = TRUE;`
3. Зареєструйте hook в `application/config/hooks.php`
4. Працює тільки в development оточенні

### 📂 WordPress - [WordpressDebugHeaders.php](examples/WordpressDebugHeaders.php)

**Готовий плагін** для WordPress:
- ✅ Версія WordPress
- ✅ Кількість SQL запитів (requires SAVEQUERIES)
- ✅ Час виконання SQL запитів
- ✅ Тип бази даних (MySQL/MariaDB)
- ✅ Час виконання
- ✅ Використання пам'яті
- ✅ Тип сторінки (single, archive, home, admin, REST API, AJAX, CRON)
- ✅ ID користувача
- ✅ Назва активної теми
- ✅ Кількість активних плагінів
- ✅ Підтримка WooCommerce та Multisite

**Як використовувати (Варіант 1 - як плагін):**
1. Створіть папку `wp-content/plugins/wordpress-debug-headers/`
2. Скопіюйте файл в цю папку
3. Активуйте плагін в WordPress Admin
4. Додайте в `wp-config.php`: `define('WP_DEBUG', true);` та `define('SAVEQUERIES', true);`

**Варіант 2 - в functions.php:**
1. Скопіюйте код (без Plugin headers) в `functions.php` вашої теми
2. Працює тільки при увімкненому WP_DEBUG

### 📂 Vanilla PHP - [PhpDebugHeaders.php](examples/PhpDebugHeaders.php)

**Універсальний клас** для будь-якого PHP проекту (без фреймворків):
- ✅ Автоматичне додавання headers при завершенні скрипта
- ✅ Підрахунок SQL запитів (ручний або автоматичний)
- ✅ Wrapper для mysqli та PDO для автоматичного підрахунку
- ✅ Автоматичне визначення типу бази даних
- ✅ Встановлення роуту вручну
- ✅ Час виконання та використання пам'яті
- ✅ Singleton паттерн для зручності

**Як використовувати:**
```php
// На початку вашого скрипта
require_once 'PhpDebugHeaders.php';
$debug = PhpDebugHeaders::init();

// Опціонально: встановіть роут
$debug->setRoute('GET /api/users');

// Автоматичний підрахунок SQL (mysqli)
$result = $debug->mysqliQuery($conn, "SELECT * FROM users");

// АБО автоматичний підрахунок SQL (PDO)
$result = $debug->pdoQuery($pdo, "SELECT * FROM users");

// АБО ручний підрахунок
$result = mysqli_query($conn, "SELECT * FROM users");
$debug->incrementQueryCount();

// Headers додаються автоматично в кінці
```

**Налаштування:** Встановіть `APP_ENV=development` в `.env` або через `putenv('APP_ENV=development')`.

---

## �📦 Встановлення

### 1. Завантаження розширення

1. Клонуйте або завантажте цей репозиторій
2. Відкрийте Chrome та перейдіть до `chrome://extensions/`
3. Увімкніть "Режим розробника" (Developer mode) у верхньому правому куті
4. Натисніть "Завантажити розпаковане" (Load unpacked)
5. Виберіть папку з розширенням

### 2. Додавання іконок

Розширення потребує іконок у папці `icons/`. Створіть PNG файли з такими розмірами:
- `icon16.png` - 16x16 пікселів
- `icon32.png` - 32x32 пікселів
- `icon48.png` - 48x48 пікселів
- `icon128.png` - 128x128 пікселів

**Швидкий спосіб**: Використайте будь-яку іконку Laravel або PHP, або створіть просту іконку з буквою "L".

## ⚙️ Налаштування серверу

### Laravel

Створіть middleware для додавання debug заголовків:

```php
// app/Http/Middleware/DebugHeaders.php
<?php

namespace App\Http\Middleware;

use Closure;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\DB;

class DebugHeaders
{
    public function handle(Request $request, Closure $next)
    {
        // Запускаємо логування запитів
        DB::enableQueryLog();
        
        $startTime = defined('LARAVEL_START') ? LARAVEL_START : microtime(true);
        
        $response = $next($request);
        
        // Додаємо debug заголовки
        $response->headers->set('X-Laravel-Version', app()->version());
        $response->headers->set('X-Laravel-Route', $request->route() ? $request->route()->getName() : 'unknown');
        $response->headers->set('X-Debug-Queries', count(DB::getQueryLog()));
        $response->headers->set('X-Debug-Time', round((microtime(true) - $startTime) * 1000, 2));
        $response->headers->set('X-Debug-Memory', round(memory_get_usage() / 1024 / 1024, 2));
        
        return $response;
    }
}
```

Зареєструйте middleware в `app/Http/Kernel.php`:

```php
protected $middleware = [
    // ... інші middleware
    \App\Http\Middleware\DebugHeaders::class, // Додайте цей рядок (тільки для local env!)
];
```

**Важливо**: Використовуйте це middleware тільки в `local` оточенні!

```php
// У app/Http/Kernel.php
protected $middleware = [
    // ...
];

// Або умовно в AppServiceProvider
if (app()->environment('local')) {
    app(\Illuminate\Contracts\Http\Kernel::class)
        ->pushMiddleware(\App\Http\Middleware\DebugHeaders::class);
}
```

### Чистий PHP (Vanilla PHP)

Для проектів без фреймворків використовуйте простий клас:

```php
// Завантажте клас на початку вашого скрипта
require_once 'PhpDebugHeaders.php';

// Ініціалізуйте debug
$debug = PhpDebugHeaders::init();

// Опціонально: встановіть роут
$debug->setRoute('GET /api/users');

// Ваш код тут...
$users = $pdo->query("SELECT * FROM users");
$debug->incrementQueryCount(); // Підрахунок SQL запитів

// Headers додаються автоматично в кінці скрипта
```

**Автоматичний підрахунок SQL** (mysqli):
```php
$debug = PhpDebugHeaders::init();
$conn = mysqli_connect('localhost', 'user', 'pass', 'db');

// Використовуйте wrapper замість mysqli_query
$result = $debug->mysqliQuery($conn, "SELECT * FROM users");
// SQL запити рахуються автоматично!
```

**Автоматичний підрахунок SQL** (PDO):
```php
$debug = PhpDebugHeaders::init();
$pdo = new PDO('mysql:host=localhost;dbname=test', 'user', 'pass');

// Використовуйте wrapper замість $pdo->query()
$result = $debug->pdoQuery($pdo, "SELECT * FROM users");
// SQL запити рахуються автоматично!
```

Повний приклад дивіться в `examples/PhpDebugHeaders.php`

**Важливо**: Встановіть `APP_ENV=development` в .env або через `putenv()` щоб headers працювали.

### CodeIgniter 4

Створіть Filter:

```php
// app/Filters/DebugHeaders.php
<?php

namespace App\Filters;

use CodeIgniter\HTTP\RequestInterface;
use CodeIgniter\HTTP\ResponseInterface;
use CodeIgniter\Filters\FilterInterface;

class DebugHeaders implements FilterInterface
{
    public function before(RequestInterface $request, $arguments = null)
    {
        // Нічого не робимо до запиту
    }

    public function after(RequestInterface $request, ResponseInterface $response, $arguments = null)
    {
        if (ENVIRONMENT === 'development') {
            $response->setHeader('X-CodeIgniter-Version', \CodeIgniter\CodeIgniter::CI_VERSION);
            $response->setHeader('X-Debug-Time', round((microtime(true) - $_SERVER['REQUEST_TIME_FLOAT']) * 1000, 2));
            $response->setHeader('X-Debug-Memory', round(memory_get_usage() / 1024 / 1024, 2));
            
            // Якщо використовуєте Database
            $db = \Config\Database::connect();
            if ($db) {
                $response->setHeader('X-Debug-Queries', count($db->getQueries()));
            }
        }
        
        return $response;
    }
}
```

Зареєструйте в `app/Config/Filters.php`:

```php
public $globals = [
    'before' => [],
    'after' => [
        'debugheaders' => ['except' => []],
    ],
];

public $aliases = [
    // ... інші фільтри
    'debugheaders' => \App\Filters\DebugHeaders::class,
];
```

### WordPress

WordPress використовує плагін або код в functions.php теми:

#### Варіант 1: Як плагін (рекомендується)

1. Створіть папку `wp-content/plugins/wordpress-debug-headers/`
2. Скопіюйте файл `examples/WordpressDebugHeaders.php` в цю папку
3. Активуйте плагін в WordPress Admin → Plugins
4. Переконайтесь що в `wp-config.php` увімкнено:

```php
define('WP_DEBUG', true);
define('SAVEQUERIES', true); // для підрахунку SQL запитів
```

#### Варіант 2: В файлі теми

Додайте в кінець `functions.php` вашої активної теми:

```php
// wp-content/themes/your-theme/functions.php

// Завантажуємо debug headers
require_once __DIR__ . '/wordpress-debug-headers.php';
```

**Що отримаєте:**

```
X-WordPress-Version: 6.4.2
X-Debug-Time: 145.23              # мс
X-Debug-Memory: 3.5               # MB
X-Debug-Queries: 12               # кількість SQL запитів
X-Debug-Database: MySQL
X-Debug-Route: single/post        # тип сторінки
X-Debug-User-Id: 1
X-Debug-Theme: Twenty Twenty-Four
X-Debug-Plugins-Count: 15
X-Debug-Request-Type: HTTP       # або REST-API, AJAX, CRON
```

**Підтримка:**
- ✅ WordPress core (всі версії 5.0+)
- ✅ WooCommerce
- ✅ Multisite
- ✅ REST API
- ✅ AJAX запити
- ✅ MySQL і MariaDB

Повний приклад в `examples/WordpressDebugHeaders.php`.

### CodeIgniter 3

Створіть Hook:

```php
// application/hooks/DebugHeaders.php
<?php
defined('BASEPATH') OR exit('No direct script access allowed');

class DebugHeaders
{
    public function set_headers()
    {
        if (ENVIRONMENT === 'development') {
            $CI =& get_instance();
            
            header('X-CodeIgniter-Version: ' . CI_VERSION);
            header('X-Debug-Time: ' . round((microtime(true) - $_SERVER['REQUEST_TIME_FLOAT']) * 1000, 2));
            header('X-Debug-Memory: ' . round(memory_get_usage() / 1024 / 1024, 2));
            
            // Якщо використовується база даних
            if (isset($CI->db)) {
                $queries = $CI->db->queries;
                header('X-Debug-Queries: ' . count($queries));
            }
        }
    }
}
```

Увімкніть hooks в `application/config/config.php`:

```php
$config['enable_hooks'] = TRUE;
```

І зареєструйте hook в `application/config/hooks.php`:

```php
$hook['post_controller'] = array(
    'class'    => 'DebugHeaders',
    'function' => 'set_headers',
    'filename' => 'DebugHeaders.php',
    'filepath' => 'hooks'
);
```

## 🎯 Використання

1. Встановіть розширення в Chrome
2. Налаштуйте ваш Laravel/CodeIgniter/WordPress проект (додайте middleware/filter/hook/плагін)
3. Відкрийте ваш локальний сайт
4. Натисніть на іконку розширення або відкрийте Side Panel (Ctrl+Shift+E або Cmd+Shift+E на Mac)
5. Дивіться інформацію про запити в реальному часі!

## 📊 Що відображається

### Summary Cards
- **Запитів** - загальна кількість перехоплених HTTP запитів
- **SQL запитів** - сумарна кількість SQL запитів
- **Середній час** - середній час виконання запитів

### Поточна сторінка
- Фреймворк та його версія
- Тип бази даних (MySQL, PostgreSQL, SQLite, SQL Server, Oracle)
- Активний роут
- Кількість SQL запитів
- Час виконання
- Використання пам'яті
- HTTP метод

### Історія запитів
- Список останніх 100 запитів
- Деталі кожного запиту
- Можливість очищення історії

## 🔧 Підтримувані заголовки

Розширення шукає такі HTTP заголовки:

- `X-Laravel-Version` / `X-CodeIgniter-Version` / `X-WordPress-Version` / `X-Framework-Version` / `X-Debug-Framework`
- `X-Laravel-Route` / `X-Debug-Route`
- `X-Laravel-Queries` / `X-Debug-Queries`
- `X-Laravel-Time` / `X-Debug-Time`
- `X-Laravel-Memory` / `X-Debug-Memory`
- `X-Debug-Database` - тип бази даних (MySQL, MariaDB, PostgreSQL, SQLite, SQL Server, Oracle)

Ви можете додати будь-які кастомні заголовки, які починаються з `X-Debug-`, `X-Laravel-`, `X-CodeIgniter-` або `X-WordPress-`.

## 🎨 Переваги перед Laravel Debugbar

1. **Не змінює DOM** - не втручається в структуру вашого сайту
2. **Не перекриває контент** - вся інформація в окремій панелі
3. **Зручність** - завжди доступна інформація без прокрутки сторінки
4. **Продуктивність** - мінімальний вплив на роботу сайту
5. **Універсальність** - працює з Laravel, CodeIgniter, WordPress та будь-яким PHP фреймворком
6. **Багатомовність** - підтримка української, англійської та російської мов

## 🌍 Багатомовність

Розширення автоматично визначає мову на основі налаштувань вашого браузера Chrome і підтримує:

- 🇬🇧 **English** - English interface
- 🇺🇦 **Українська** - Український інтерфейс
- 🇷🇺 **Русский** - Русский интерфейс

### Зміна мови

Мова розширення змінюється автоматично відповідно до мови браузера:

1. Відкрийте `chrome://settings/languages`
2. Додайте потрібну мову або перемістіть її на перше місце
3. Перезапустіть Chrome

### Додавання нових мов

Детальні інструкції по додаванню підтримки нових мов дивіться у файлі [I18N.md](I18N.md).

## 🚀 Розширені можливості

### Додавання кастомних метрик

Ви можете додати будь-які кастомні метрики через заголовки:

```php
$response->headers->set('X-Debug-Cache-Hits', $cacheHits);
$response->headers->set('X-Debug-Api-Calls', $apiCalls);
$response->headers->set('X-Debug-User-Id', auth()->id());
```

Розширення автоматично відобразить всі заголовки, які починаються з `X-Debug-`.

## 🐛 Troubleshooting

### Розширення не показує дані

1. Перевірте, чи middleware/filter/hook правильно зареєстрований
2. Перевірте, що ви в `local` або `development` оточенні
3. Відкрийте DevTools (F12) і подивіться на заголовки відповіді (Network → Headers)
4. Переконайтесь, що заголовки встановлюються (мають бути в Response Headers)

### Side Panel не відкривається

1. Переконайтесь, що у вас Chrome версії 114 або новіше
2. Спробуйте натиснути на іконку розширення
3. Або використайте меню Chrome → More Tools → Side Panel

## 📝 TODO / Майбутні покращення

- [ ] Експорт даних в JSON/CSV
- [ ] Фільтрація запитів за типом
- [ ] Детальний перегляд SQL запитів (якщо сервер їх надає)
- [ ] Графіки та статистика
- [ ] Підтримка Symfony та інших PHP фреймворків
- [ ] Темна/світла тема (зараз тільки темна)
- [ ] Сповіщення про повільні запити

## 📄 Ліцензія

MIT License - використовуйте вільно!

## 🤝 Внесок

Вітається! Створюйте Pull Requests з покращеннями.

---

**Примітка**: Це розширення призначене для розробки. Ніколи не використовуйте debug заголовки на production серверах!
